// eslint-disable-next-line no-shadow
export enum FetchDataMethods {
    POST = 'POST',
    GET = 'GET',
    DELETE = 'DELETE',
}

// eslint-disable-next-line no-shadow
export enum FloatOptions {
    LEFT = 'LEFT',
    RIGHT = 'RIGHT',
}
